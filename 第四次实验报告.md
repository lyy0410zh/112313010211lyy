# 交通标志检测实验报告

**姓名：刘悦悦
**学号：112313010211

## 1. 实验目标
本实验使用 YOLO 模型完成交通标志目标检测任务，并对模型训练过程和检测结果进行分析。

---

## 2. 实验环境
- 操作系统：Windows
- Python 版本：3.7.3
- PyTorch 版本：1.13.1+cpu
- YOLO 版本：YOLOv8.0.145
- 硬件环境（CPU / GPU）：CPU (13th Gen Intel Core(TM) i5-1340P)

---

## 3. 模型与训练设置
### 3.1 模型选择
本实验使用的模型为：
- 模型名称：YOLOv8n (YOLOv8 Nano)
- 选择该模型的原因：YOLOv8n 是 YOLOv8 系列中最小的模型，参数量仅 3M，适合在 CPU 上训练和推理，同时保持了较好的检测精度。对于交通标志检测任务，该模型能够在速度和精度之间取得良好的平衡。

### 3.2 训练参数
- 训练轮数（epochs）：30
- 图像尺寸（imgsz）：640
- batch size：16
- 优化器：AdamW
- 学习率：初始学习率 0.01，最终学习率衰减至约 0.00004
- 是否使用数据增强：是（包括 HSV 颜色空间增强、随机翻转、Mosaic 数据增强等）

### 3.3 训练命令
```bash
python train_complete.py
```

---

## 4. 训练过程分析

### 4.1 损失曲线
在此插入训练损失图（如 `box_loss`、`cls_loss`、`dfl_loss` 等）。

![损失曲线](runs/detect/traffic_signs_complete/results.png)

请结合图像回答以下问题：
1. 损失是否总体下降？
   - 是，所有损失函数（box_loss、cls_loss、dfl_loss）都呈现明显的下降趋势。box_loss 从 0.844 下降到 0.500，cls_loss 从 3.471 下降到 0.381，dfl_loss 从 1.186 下降到 0.906。

2. 哪一阶段下降最快？
   - 前 5 个 epoch 下降最快，这是模型从预训练权重快速适应新任务的过程。

3. 后期是否趋于稳定？
   - 是，在 20 个 epoch 之后，损失曲线趋于稳定，变化幅度较小，说明模型已经基本收敛。

4. 是否出现明显震荡或过拟合现象？
   - 没有出现明显震荡，训练过程平稳。验证集损失与训练集损失趋势一致，未出现明显过拟合现象。

### 4.2 评价指标变化
在此插入 `Precision`、`Recall`、`mAP50`、`mAP50-95` 等曲线图。

![指标曲线](runs/detect/traffic_signs_complete/results.png)

请简要分析：
1. 哪个指标提升最明显？
   - mAP50 提升最明显，从第 1 个 epoch 的 0.247 提升到最终的 0.957，提升了 71%。Precision 从 0.218 提升到 0.949，Recall 从 0.480 提升到 0.890。

2. 最终模型效果如何？
   - 最终模型效果非常好，mAP50 达到 0.957，mAP50-95 达到 0.833，Precision 达到 0.949，Recall 达到 0.890。这表明模型在交通标志检测任务上具有很高的准确性和召回率。

3. 模型是否已经基本收敛？
   - 是，从第 20 个 epoch 开始，各项指标趋于稳定，mAP50 在 0.94-0.96 之间波动，说明模型已经基本收敛。

---

## 5. 混淆矩阵分析

在此插入混淆矩阵图。

![混淆矩阵](runs/detect/traffic_signs_complete/confusion_matrix.png)

请重点分析：
1. 哪些类别识别效果最好？
   - Speed Limit 120、Speed Limit 20、Speed Limit 30、Speed Limit 70、Stop 等类别识别效果最好，准确率都在 95% 以上。这些类别的标志形状和颜色特征明显，易于区分。

2. 哪些类别最容易混淆？
   - Green Light 和 Red Light 之间存在一定混淆，主要是因为交通灯在不同状态下的形状相似，仅颜色不同。部分速度限制标志（如 Speed Limit 50 和 Speed Limit 60）也存在轻微混淆。

3. 造成类别混淆的可能原因是什么？
   - 类间相似性：某些类别的标志外观相似，如不同数值的速度限制标志。
   - 数据分布不均：某些类别样本数量较少，如 Speed Limit 10 和 Speed Limit 110。
   - 图像质量：部分图像存在模糊、遮挡或光照不足等问题。

4. 从混淆矩阵中可以看出模型还有哪些不足？
   - 对于外观相似的类别，模型的区分能力还有提升空间。
   - 对于样本数量较少的类别，模型可能学习不够充分。

---

## 6. 检测结果分析

在此插入若干预测结果图（建议 2 到 4 张）。

![预测结果1](runs/detect/traffic_signs_complete/val_batch0_pred.jpg)
![预测结果2](runs/detect/traffic_signs_complete/val_batch1_pred.jpg)

请分析：
1. 哪些目标检测较准确？
   - 大部分交通标志检测准确，尤其是速度限制标志和 Stop 标志。模型能够准确识别标志类别并给出高置信度的预测框。

2. 哪些图片中存在漏检或误检？
   - 在部分复杂场景中，存在少量漏检现象，主要是一些小目标或被遮挡的标志。
   - 在光照条件较差的图像中，偶有误检情况。

3. 误检、漏检的可能原因是什么？
   - 小目标：部分交通标志在图像中占比较小，特征不明显。
   - 遮挡：部分标志被其他物体遮挡，导致特征不完整。
   - 光照：强光或阴影条件下，标志特征受到影响。

4. 小目标、遮挡目标、远距离目标的检测效果如何？
   - 小目标检测效果一般，存在一定漏检。
   - 遮挡目标的检测效果取决于遮挡程度，轻微遮挡可以检测，严重遮挡容易漏检。
   - 远距离目标（小目标）检测效果有待提升。

---

## 7. 提交成绩
- 本地验证结果：mAP50 = 0.957, mAP50-95 = 0.833
- 比赛网站提交分数：（待提交后填写）
- 当前排行榜名次（如有）：（待提交后填写）

请简要说明：
1. 本地验证结果与提交分数是否一致？
   - （待提交后填写）

2. 如果不一致，可能原因是什么？
   - 测试集与验证集的数据分布可能存在差异。
   - 测试集可能包含更多困难样本。

---

## 8. 实验总结
请用简短几句话总结本次实验：
1. 本次实验中模型的主要优点是什么？
   - 模型体积小（仅 6.2MB），适合部署。
   - 检测精度高，mAP50 达到 0.957。
   - 训练收敛快，30 个 epoch 即可达到较好效果。
   - 推理速度快，适合实时检测应用。

2. 当前模型最明显的问题是什么？
   - 小目标检测能力有待提升。
   - 对于外观相似的类别（如不同数值的速度限制标志），区分能力还需加强。
   - 在复杂场景（遮挡、光照不足）下的鲁棒性还需改进。

3. 如果继续改进，你下一步会尝试什么方法？
   - 使用更大的模型（如 YOLOv8s 或 YOLOv8m）以提升检测精度。
   - 增加训练轮数或使用更多数据增强方法。
   - 针对小目标优化，如使用多尺度训练或增加小目标检测头。
   - 使用测试时增强（TTA）提升检测效果。
   - 尝试模型集成方法，结合多个模型的预测结果。
